# Changesets

This directory tracks release intent for `loom`.

- add a new markdown file with `bun run changeset`
- commit it with the related code change
- run `bun run version-packages` when you're ready to cut a release version
